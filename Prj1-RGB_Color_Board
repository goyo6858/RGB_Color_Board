#基于GUI开展架构设计

#---------------------------------------------------------功能------------------------------------------------------
'''
目标：
1.保存文档
2.导入文档
3.颜色收藏功能
待解决问题：
1. 窗口生成后自动最大化-已解决(2021-5-25)
2. 替换窗口图标-已解决（2021-5-25）
3. 拖动并放大窗口
'''
#-------------------------------------------------------------------------------------------------------------------

#-----------------------------导入模块-----------------------------
#------------------------------------------------------------------

from tkinter import *
from random import randint

#------------------------------变量--------------------------------
'''
'''
#------------------------------------------------------------------
#color=''#字符串表示的颜色值，格式为16进制#XXXXXX
color_now='' #当前颜色-16进制

#------------------------------函数---------------------------------
'''
color():返回16进制颜色值
cvs_move():拖动及放大画
color_change():变化画布颜色
read_color():获取当前画布颜色
'''
#-------------------------------------------------------------------
def rgb_hex(r,g,b):
  return '#{:02X}{:02X}{:02X}'.format(r,g,b)
  
def hex_rgb(color): #参数为'#XXXXXX
  return '{},{},{}'.format(int(color[1:3],16),int(color[3:5],16),int(color[5:7],16)) #字符串格式化

def get_color(): #临时调试用按钮，改变label text等
  color_now=cvs1.cget('bg')
  color_now_rgb=hex_rgb(color_now)
  lb_color_now.configure(text='Color:'+color_now_rgb)
  print('Color_now:',color_now)
  print('RGB:',color_now_rgb)

#-------------------------------窗口--------------------------------
root=Tk()
root.title('RGB Color Board') 
root.state('zoomed') #最大化
root.iconbitmap(r'D:\Python\Projects\RGB_Color_Board\images\Icon.ico') #更换图标
root.attributes('-alpha',1) #透明度

#------------------------------对象---------------------------------
'''
1. Canvas
2. scale widgets
3. 无法实现？模仿微软画图硬件-鼠标进入区域后，滚轮调整改变RGB值或色温等
'''
#--------------------------------------------------------------------

lb_color_now=Label(text='Color:'+color_now)
cvs1=Canvas(width=200,height=200,bg=rgb_hex(255,255,255))
bt1=Button(text='Get color',command=get_color)

#------------------------------布局----------------------------------
lb_color_now.pack(side=TOP)
cvs1.pack()
bt1.pack(side=BOTTOM)

#------------------------------循环------------------------------------
'''
以下赋值代码不循环？print可以输出值，但label的
'''
color_now=cvs1.cget('bg')
color_now_rgb=hex_rgb(color_now)
mainloop()
